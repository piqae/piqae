import { render } from "preact";

import { AdminOrderQuickPrintAction } from "../../shared/AdminOrderQuickPrintAction.jsx";

export default async () => {
  render(<AdminOrderQuickPrintAction />, document.body);
};
