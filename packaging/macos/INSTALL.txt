Install Piqae
=============

Double-click “Install Piqae” to install the menu app and local printing node
for your macOS user.

Piqae installs without administrator access. Before an update it verifies that
the local queue is idle, and it preserves your device identity, printer
profiles, and durable local queue.

Requirements: macOS 13 or newer.
