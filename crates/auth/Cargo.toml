[package]
name = "piqae-auth"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
argon2 = "0.5"
base64 = "0.22"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
uuid = { version = "1", features = ["serde", "v7"] }
