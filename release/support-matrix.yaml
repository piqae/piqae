version: 1
platforms:
  windows_10_11_x86_64:
    tier: disabled
    reason: physical PDF and RAW release gates have not run
  macos_13_plus_aarch64:
    tier: preview
    reason: v0.1.11 is Developer ID signed, Apple notarised and stapled with SBOM, checksum and repository-bound provenance; clean-install, update/rollback and physical printing gates remain open
  macos_15_x86_64:
    tier: preview
    reason: v0.1.11 is Developer ID signed, Apple notarised and stapled with SBOM, checksum and repository-bound provenance; clean-install, update/rollback and physical printing gates remain open
  ubuntu_22_04_24_04_x86_64:
    tier: preview
    reason: v0.1.11 x86_64 packaging, checksum, SBOM and provenance evidence exists; clean-install, upgrade/rollback and physical printing gates have not run
  debian_12_x86_64:
    tier: preview
    reason: v0.1.11 x86_64 packaging, checksum, SBOM and provenance evidence exists; clean-install, upgrade/rollback and physical printing gates have not run
  linux_aarch64:
    tier: preview
    reason: v0.1.11 aarch64 packaging, checksum, SBOM and provenance evidence exists; clean-install, upgrade/rollback and low-power hardware gates have not run
features:
  durable_job_registration:
    tier: preview
    reason: durable database and object-store paths are tested locally but have no production soak evidence
  offline_node_delivery:
    tier: preview
    reason: lease expiry and reconnect paths are tested, but long-running fleet evidence is incomplete
  primary_standby_target_routing:
    tier: preview
    reason: new and waiting jobs can select a fresh compatible route before native handoff, but physical-printer redundancy and fleet duplicate-prevention soak evidence are incomplete
  automatic_waiting_job_reassignment:
    tier: preview
    reason: atomic fenced pre-handoff reassignment is implemented for compatible direct and target jobs, but physical fault injection and fleet duplicate-prevention soak evidence are incomplete
  physical_destination_route_coordination:
    tier: preview
    reason: tenant-scoped destinations, multi-node routes, fenced handoff reservations, reversible identity decisions, and the node-local coordinator have PostgreSQL and non-physical evidence; physical redundancy, upgrade-fleet, and cross-authority soak gates remain open
  live_route_telemetry:
    tier: preview
    reason: authenticated freshness-bounded route health and privacy-safe spooler occupancy are exposed, but vendor status accuracy, long-running reconciliation, and physical stock/profile evidence remain incomplete
  compose_self_hosting:
    tier: preview
    reason: configuration renders locally but backup, restore, and upgrade drills are operator-owned
  kubernetes_self_hosting:
    tier: preview
    reason: Helm policy renders and validates locally but Kind disruption and rollback evidence is incomplete
  signed_automatic_node_updates:
    tier: disabled
    reason: signed coordinated app/node updates and component rollback are implemented, but destructive fault-injection and physical-printer certification remain release gates
  legacy_compatibility:
    tier: preview
    reason: only the documented compatibility subset is covered by fixtures
  platform_service_accounts:
    tier: disabled
    reason: cross-resource/concurrency isolation, operator attribution, selector rejection, and full redaction evidence are incomplete
  multi_integrator_node_runtime:
    tier: preview
    reason: isolated connector workers plus OS-verified macOS Unix-socket and Windows named-pipe consent are implemented in source, but signed current SDK packages, Linux native consent, fleet concurrency and revocation soak evidence are incomplete
  embeddable_node_runtime:
    tier: preview
    reason: the shared runtime, versioned broker and native ABI have deterministic local-only, connector-isolation, lifecycle and restart evidence; signed SDK artifacts, host-app fleet soak and physical printing remain release gates
  apple_nodekit:
    tier: preview
    reason: the source Swift package exposes OS-verified fail-closed protocol-v4 installed-node consent plus app-scoped APIs with Keychain-backed connector signing, coalesced automatic durable-work drain, foreground-safe AirPrint and lifecycle tests; signed binary distribution, App Store review, accessory entitlements, prolonged kiosk and physical printer evidence remain incomplete
  windows_node_sdk:
    tier: disabled
    reason: the native ABI, managed facade, Credential Manager protected-at-rest Ed25519 fallback and OS-verified named-pipe consent are implemented in source; hosted Windows CI, signed NuGet publication, suspend-resume and physical Winspool evidence remain release gates
  unattended_ipados_printing:
    tier: disabled
    reason: iPadOS background execution and silent notifications are opportunistic; no force-quit or sleeping-device guarantee exists, and APNs, managed-kiosk, BLE, MFi, vendor-adapter and physical soak gates remain open
  external_node_wake_relay:
    tier: disabled
    reason: wake hints are durable and never carry job content or leases, but only already-connected session delivery is verified; APNs and LAN wake dispatchers, tenant-bound registration, rate limiting and hardware wake evidence remain open
  mobile_printer_adapter_registry:
    tier: preview
    reason: exact adapter-version, transport, device-family and firmware matching is bounded and fail-closed; vendor licensing, entitlement review and physical option-mapping certification remain adapter-specific release gates
  content_confidential_printing:
    tier: disabled
    reason: encrypted transport and local decryption are implemented for evaluation but lack independent cryptographic review, hardware-backed keys and physical release evidence
  declarative_document_generation:
    tier: disabled
    reason: the bounded business-document renderer, encrypted tenant persistence, SDK/MCP contracts, content-addressed JPEG resources, optional exact node rendering, PostgreSQL cutover tests, virtual lifecycle tests, and a 250-page local synthetic benchmark exist; international Unicode shaping, downloadable fonts, PNG/SVG, production fleet soak, independent security review, and physical release gates remain open
  shopify_order_printing:
    tier: disabled
    reason: the embedded app, Word-like semantic editor, Liquid compiler, Admin/POS/customer extensions, render-policy/readiness controls, verified asset publication, direct-node path, and PDF fallback pass local and virtual gates; international typography, deployment of the new API, Shopify approvals, live bulk store/POS validation, Web Vitals evidence, production fleet soak, and physical printing remain open
  macos_embedded_node_onboarding:
    tier: preview
    reason: OS-verified Unix-socket peer identity, Piqae-branded consent and protocol-v4 proof-of-possession are tested in source; the current embedded SDK candidate is unsigned and its clean-install, update/rollback, App Store and fleet evidence remain open
  windows_embedded_node_onboarding:
    tier: disabled
    reason: OS-verified named-pipe peer identity, local consent and protocol-v4 proof-of-possession are implemented in source; hosted Windows CI, Authenticode-signed packaging, install/update/rollback and fleet evidence remain open
  linux_embedded_node_onboarding:
    tier: disabled
    reason: only the shared headless enrollment transaction exists; a native OS-verified application identity, application-link registration and local consent shell are not implemented
