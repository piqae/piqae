gcp_project_id = "replace-production-project"
environment    = "production"
image          = "australia-southeast1-docker.pkg.dev/replace/piqae/server@sha256:replace"
migration_image = "australia-southeast1-docker.pkg.dev/replace/piqae/migrate@sha256:replace"

# Supply secrets through an encrypted tfvars mechanism; never commit values.
# Managed Cloud SQL creates the piqae database/user and authenticated connector
# URL, so no database_url_secret is needed.
object_store_endpoint            = "https://replace.r2.cloudflarestorage.com"
object_store_bucket              = "piqae-production"
object_store_access_key_secret   = "replace-at-apply"
object_store_secret_key_secret   = "replace-at-apply"
webhook_master_key_secret        = "replace-at-apply"
destination_identity_key_secret  = "replace-at-apply"
document_master_key_secret       = "replace-at-apply"
stripe_secret_key_secret          = "replace-at-apply"
stripe_webhook_secret             = "replace-at-apply"
stripe_meter_event_name           = "piqae_jobs_accepted"
oidc_jwks_url                    = "https://replace.example.com/.well-known/jwks.json"
oidc_binding_value               = "client_replace"

enable_multi_region             = true
secondary_region               = "australia-southeast2"
secondary_min_instances        = 1
enable_global_load_balancer    = true
allow_public_cloud_run_invocation = true
load_balancer_domains          = ["api.piqae.example.com"]

enable_managed_data_plane   = true
managed_object_bucket_name  = "replace-globally-unique-piqae-production"
