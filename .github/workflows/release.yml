name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Version label used for a dry-run artifact build
        required: true
        default: "v0.1.0-dev"

permissions:
  contents: read

jobs:
  native:
    name: Native (${{ matrix.artifact }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: spool-windows-x86_64
            extension: .exe
            extra_packages: -p spool-executor-windows -p spool-shell-windows
            executor_binary: spool-executor-windows
            shell_binary: spool-shell-windows
          - os: macos-15
            target: aarch64-apple-darwin
            artifact: spool-macos-aarch64
            extension: ""
            extra_packages: -p spool-executor-cups
            executor_binary: spool-executor-cups
            shell_binary: ""
          - os: macos-15-intel
            target: x86_64-apple-darwin
            artifact: spool-macos-x86_64
            extension: ""
            extra_packages: -p spool-executor-cups
            executor_binary: spool-executor-cups
            shell_binary: ""
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: spool-linux-x86_64
            extension: ""
            extra_packages: -p spool-executor-cups -p spool-shell-linux
            executor_binary: spool-executor-cups
            shell_binary: spool-shell-linux
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            artifact: spool-linux-aarch64
            extension: ""
            extra_packages: -p spool-executor-cups -p spool-shell-linux
            executor_binary: spool-executor-cups
            shell_binary: spool-shell-linux
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.88.0
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.target }}
      - name: Install Linux CUPS development library
        if: runner.os == 'Linux'
        shell: bash
        run: sudo apt-get update && sudo apt-get install --yes libcups2-dev
      - name: Build native binaries
        shell: bash
        run: cargo build --locked --release --target "${{ matrix.target }}" -p spool-agent -p spoolctl ${{ matrix.extra_packages }}
      - name: Build macOS menu shell
        if: runner.os == 'macOS'
        shell: bash
        run: swift build --configuration release --package-path shells/macos
      - name: Stage native bundle
        shell: bash
        run: |
          mkdir -p "artifacts/${{ matrix.artifact }}/docs" "artifacts/${{ matrix.artifact }}/service"
          cp "target/${{ matrix.target }}/release/spool-agent${{ matrix.extension }}" "artifacts/${{ matrix.artifact }}/"
          cp "target/${{ matrix.target }}/release/spoolctl${{ matrix.extension }}" "artifacts/${{ matrix.artifact }}/"
          cp "target/${{ matrix.target }}/release/${{ matrix.executor_binary }}${{ matrix.extension }}" "artifacts/${{ matrix.artifact }}/"
          if [ -n "${{ matrix.shell_binary }}" ]; then
            cp "target/${{ matrix.target }}/release/${{ matrix.shell_binary }}${{ matrix.extension }}" "artifacts/${{ matrix.artifact }}/"
          fi
          if [ "${{ runner.os }}" = "macOS" ]; then
            cp "shells/macos/.build/release/SpoolMenu" "artifacts/${{ matrix.artifact }}/"
          fi
          cp README.md LICENSE "artifacts/${{ matrix.artifact }}/"
          cp -R LICENSES "artifacts/${{ matrix.artifact }}/"
          cp packaging/README.md "artifacts/${{ matrix.artifact }}/PACKAGING.md"
          cp release/native-bundle-status.md "artifacts/${{ matrix.artifact }}/NATIVE-BUNDLE-STATUS.md"
          cp release/support-matrix.yaml "artifacts/${{ matrix.artifact }}/SUPPORT-MATRIX.yaml"
          cp docs/operations/agent-service-installation.md "artifacts/${{ matrix.artifact }}/docs/"
          case "${{ runner.os }}" in
            Linux) cp -R packaging/linux/. "artifacts/${{ matrix.artifact }}/service/" ;;
            macOS) cp -R packaging/macos/. "artifacts/${{ matrix.artifact }}/service/" ;;
            Windows) cp -R packaging/windows/. "artifacts/${{ matrix.artifact }}/service/" ;;
          esac
      - name: Generate native SPDX SBOM
        uses: anchore/sbom-action@v0
        with:
          path: artifacts/${{ matrix.artifact }}
          format: spdx-json
          output-file: artifacts/${{ matrix.artifact }}/SBOM.spdx.json
          upload-artifact: false
          upload-release-assets: false
      - name: Archive Unix bundle
        if: runner.os != 'Windows'
        shell: bash
        run: tar -C artifacts -czf "artifacts/${{ matrix.artifact }}.tar.gz" "${{ matrix.artifact }}"
      - name: Archive Windows bundle
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path "artifacts/${{ matrix.artifact }}" -DestinationPath "artifacts/${{ matrix.artifact }}.zip"
      - name: Write Unix bundle checksum
        if: runner.os != 'Windows'
        shell: bash
        working-directory: artifacts
        run: |
          archive="${{ matrix.artifact }}.tar.gz"
          shasum -a 256 "$archive" > "$archive.sha256"
      - name: Write Windows bundle checksum
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $archive = "artifacts/${{ matrix.artifact }}.zip"
          $hash = (Get-FileHash -Algorithm SHA256 $archive).Hash.ToLowerInvariant()
          "$hash  $([System.IO.Path]::GetFileName($archive))" |
            Out-File -Encoding ascii "$archive.sha256"
      - uses: actions/attest-build-provenance@v2
        with:
          subject-path: artifacts/${{ matrix.artifact }}.*
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: artifacts/${{ matrix.artifact }}.*
          if-no-files-found: error

  server:
    name: Container (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: server
            dockerfile: deploy/docker/Dockerfile.server
          - name: web
            dockerfile: deploy/docker/Dockerfile.web
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: startsWith(github.ref, 'refs/tags/')
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: metadata
        with:
          images: ghcr.io/${{ github.repository }}/${{ matrix.name }}
          tags: |
            type=ref,event=tag
            type=sha
      - uses: docker/build-push-action@v6
        id: build
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: ${{ startsWith(github.ref, 'refs/tags/') }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
      - uses: actions/attest-build-provenance@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          subject-name: ghcr.io/${{ github.repository }}/${{ matrix.name }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

  github-release:
    name: Publish GitHub release
    if: startsWith(github.ref, 'refs/tags/')
    needs: [native, server]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: spool-*
          path: artifacts
          merge-multiple: true
      - name: Write checksums
        working-directory: artifacts
        run: sha256sum spool-* > SHA256SUMS
      - name: Publish immutable release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${GITHUB_REF_NAME}" artifacts/* --generate-notes --verify-tag
