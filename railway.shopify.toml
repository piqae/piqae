[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/docker/Dockerfile.shopify"

[deploy]
preDeployCommand = "node scripts/migrate-postgres.mjs"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
